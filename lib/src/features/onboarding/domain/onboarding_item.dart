class OnboardingItem {
  final String title;
  final String description;
  final String imagePath; // Placeholder, in real app would use assets

  OnboardingItem({
    required this.title,
    required this.description,
    required this.imagePath,
  });
}
